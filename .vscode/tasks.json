{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "A_Make CMakeLists.txt",
			"type": "process",
			"command": "cmake",
			"args": [
				"-P CMakeTemplateByTanakaDorei.cmake",
			],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		},
		{
			"label": "B_CMake Configure",
			"type": "process",
			"command": "cmake",
			"args": [
				"-P CMakeListsExecutor.cmake",
			],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}
